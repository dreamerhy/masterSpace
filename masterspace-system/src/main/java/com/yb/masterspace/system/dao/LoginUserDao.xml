<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yb.masterspace.system.dao.LoginUserDao">

    <select id="getUserInfo" resultType="com.yb.masterspace.base.dto.SysUserInfoDto">
        SELECT ID, USERNAME, SEX, AGE FROM USER WHERE ID = #{userid}
    </select>

</mapper>